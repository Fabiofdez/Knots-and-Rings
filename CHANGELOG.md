### Changes
- Logs now heal faster overall
- Under-the-hood optimizations

### Fixes
- Fix issue with logs not splitting with pickaxe
- Tree trunk connected rings now behave consistently
- Tree state per-chunk caching now works the same across loaders
- Trees grown from saplings now immediately have connected rings
